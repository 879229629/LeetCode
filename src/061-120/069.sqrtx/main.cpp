#include <iostream>

#include "solution.h"

int main(int argc, char *argv[]) {
    solution s;
    for (int i = 0; i < 100; ++i) {
//        int ret = s.mySqrt(i);
//        std::cout << "ret: " << i << "," << ret << std::endl;
    }

    std::cout << s.mySqrt(2147395599);
}