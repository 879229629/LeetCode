cmake_minimum_required(VERSION 3.5)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")

set(SOURCE_FILES listnode.cpp treeNode.cpp treeNode.h)

add_library(common ${SOURCE_FILES})

add_executable(common.out main.cpp ${SOURCE_FILES} treelinknode.cpp treelinknode.h treeLinkNode.cpp treeLinkNode.h)
